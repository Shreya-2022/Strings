#include<iostream>
#include<string>
#include <algorithm>
using namespace std;
int main(){
    string str = "Hello Shreya";
   // reverse(str.begin(), str.end());
    reverse(str.begin(), str.end()); // Reverse the string
    cout<<str<<endl;
    return 0;

}
